{
  "name": "prompt-anonymizer-web",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@10.12.1"
}
