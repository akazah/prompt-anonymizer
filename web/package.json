{
  "name": "prompt-anonymizer-web",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "e2e": "pnpm --filter @prompt-anonymizer/e2e e2e",
    "e2e:ner": "pnpm --filter @prompt-anonymizer/e2e e2e:ner",
    "lint": "pnpm --filter @prompt-anonymizer/core build && pnpm -r lint"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@11.10.0"
}
