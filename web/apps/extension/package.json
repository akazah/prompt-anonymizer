{
  "name": "@prompt-anonymizer/extension",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/postbuild.mjs",
    "lint": "tsc --noEmit",
    "package": "npm run build && cd dist && zip -rq ../prompt-anonymizer-extension.zip ."
  },
  "dependencies": {
    "@prompt-anonymizer/core": "workspace:*",
    "@prompt-anonymizer/theme": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "typescript": "^6.0.3",
    "vite": "^8.1.3"
  }
}
