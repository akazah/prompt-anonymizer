{
  "name": "@prompt-anonymizer/desktop",
  "private": true,
  "version": "0.3.1",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "tauri": "tauri",
    "build": "echo 'desktop: native bundles are built in CI via tauri-action (run: pnpm --filter @prompt-anonymizer/desktop tauri build)'",
    "test": "vitest run",
    "lint": "node -e \"JSON.parse(require('fs').readFileSync('src-tauri/tauri.conf.json','utf8')); console.log('tauri.conf.json: OK')\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "vitest": "^4.1.9"
  }
}
