{
  "name": "@prompt-anonymizer/mcp",
  "version": "0.3.0",
  "description": "Anonymize PII before it reaches an LLM - consistent, reversible labels. MCP server (anonymize / deanonymize / scan tools) built on @prompt-anonymizer/core.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "prompt-anonymizer-mcp": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20.16"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/akazah/prompt-anonymizer.git",
    "directory": "web/packages/mcp"
  },
  "homepage": "https://github.com/akazah/prompt-anonymizer#readme",
  "bugs": {
    "url": "https://github.com/akazah/prompt-anonymizer/issues"
  },
  "keywords": [
    "pii",
    "anonymization",
    "privacy",
    "llm",
    "mcp",
    "model-context-protocol"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@prompt-anonymizer/core": "workspace:*",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
